//---------------------------------------------------------------------------
#ifndef readH
#define readH
//---------------------------------------------------------------------------
#define MW_HANDLE_REG_ERROR     \
            if (regMenuWorks->WasError == true) { WasError = true; goto CLEAN_UP; }

#endif
